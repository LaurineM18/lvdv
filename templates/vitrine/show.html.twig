{% extends 'base.html.twig' %}

{% block title %}Vitrine{% endblock %}

{% block body %}
    <h1>Vitrine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vitrine.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vitrine.Name }}</td>
            </tr>

            <tr>
                <th>New</th>
                <td>{{ vitrine.New ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ vitrine.Price }}</td>
            </tr>
            <tr>
                <th>Available</th>
                <td>{{ vitrine.Available ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ vitrine.Description }}</td>
            </tr>

        </tbody>
    </table>


    <button class="btn_admin btn_admin_edit"><a href="{{ path('app_vitrine_edit', {'id': vitrine.id}) }}">Modifier</a></button>

    {{ include('vitrine/_delete_form.html.twig') }}

    <a class="dashboard_link" href="{{ path('app_vitrine_index') }}">Retour à la liste</a>
{% endblock %}
