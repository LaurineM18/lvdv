<form method="post" action="{{ path('app_vitrine_delete', {'id': vitrine.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vitrine.id) }}">
    <button class=" btn_admin btn_admin_delete">Supprimer</button>
</form>
