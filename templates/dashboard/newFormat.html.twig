{% extends 'base.html.twig' %}

{% block title %}Nouveau format{% endblock %}

{% block body %}
<div class="dashboard_vitrines">

    <h1>Ajouter un nouveau format</h1>
    
        {{form_start(form)}}
        {{ form_widget(form) }}
        <button class="btn btn_form">Enregistrer</button>
        {{form_end(form)}}


    <a class="dashboard_link" href="{{ path('app_dashboard_index') }}">Retour au tableau de bord</a>
</div>

{% endblock %}
